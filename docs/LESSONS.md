# Lessons

A running log of what worked, what nearly broke, and the rule to remember. Append newest at the top.

## 2026-07-14 — Fluid layout: one cap was the whole problem
- **What worked:** The dead right-side canvas across every page traced to a SINGLE rule — `main { max-width: 1320px; margin: 0 auto }`. Removing the cap + centring (keeping `width:100%` and the clamp padding) made every page fluid at once. A prior fix had already solved this for the map via a per-route `.full-bleed` class; the general fix made that special-case redundant, so I generalized the KPI-card-sizing rule (that stops cards sprawling on ultra-wide) from `.full-bleed` to all pages and left the class as an inert hook.
- **What nearly broke / the gotcha:** A test (`verify-nav`) asserted "navigating away restores the readable cap" — an assertion whose *description* became a lie once there's no cap, even though the class-toggle it checked still passed. I re-pointed it to assert the real new invariant (`main` has no max-width) so the suite guards the fluid layout, not a cap that no longer exists. Left the class-toggle checks (still valid behaviour) but corrected their wording.
- **What I'd do differently next time:** When a global layout complaint comes in, grep for the ONE cap/centring rule before touching individual views — the fix is usually a single declaration, not per-page work. And when you invert a layout decision, find the test whose *comment* describes the old intent, not just the ones that fail.
- **Rule to remember:** Global layout problems usually have a single-rule root cause (`max-width` + `margin:auto` on the content container). Change it there, not per-view. A passing test with a now-false description is tech debt — re-point it at the new invariant.

## 2026-07-14 — Read the artifact TYPE before building (deck ≠ page)
- **What worked:** When Akram pasted the full deck spec, it was unambiguous: a Google Apps Script that generates a Google Slides deck (cover + your-requests + one-slide-per-wave + what-needs-you), living in Google, no repo code. I reverted the wrong in-app page cleanly (file + route + nav + card + CSS + i18n + test, suite green) and delivered the actual thing — a config-driven Code.gs + setup checklist + change-it-later one-pager.
- **What went wrong (the real lesson):** Two turns earlier I'd built an in-app "Executive Status page." The ORIGINAL B1 prompt had said the deck was "a separate Google Slides artifact" and the app work was "only the LINK" — I had that context and still built a page when Akram said "create the deck." I over-indexed on "in-app is world-class / self-updating" and talked myself past his explicit format. He was right: the page just duplicated the dashboard and showed nothing new.
- **What I'd do differently next time:** When the user specifies a DELIVERABLE TYPE (Slides deck / Apps Script / PDF / email), that's a hard constraint, not a starting point to optimize away. "Build the best version" applies to the thing they asked for, not to swapping it for a different thing I think is better. If I genuinely think another format is better, propose it in one line and let them choose — don't just build it.
- **Rule to remember:** Deliverable format is the user's call, stated explicitly = locked. Reread the original spec's nouns ("deck", "slide", "Apps Script", "link") before choosing an architecture. A self-updating in-app view is not automatically better than the artifact they asked for — especially when it duplicates something that already exists.

## 2026-07-14 — Executive Status: built it in-app (SUPERSEDED — was the wrong call)
- **SUPERSEDED by the lesson above.** I built the deck as a live in-app page here, reasoning "the data's already in the app, so build the view not a link." That reasoning is fine in general BUT it ignored Akram's explicitly stated deliverable type (a Google Slides deck). The page duplicated the dashboard and he rejected it; it was reverted in #93. Kept for the trail — the takeaway is the SUPERSEDED note + the "read the artifact TYPE" lesson above, not the reasoning below.
- **Salvageable technical bit:** when you change what a control does (external link → internal nav), update its element type (`<a>`→`<button>`), its CSS assumptions (a button needs `font:inherit;cursor:pointer;width:100%;text-align:start`), AND invert its test. That part held up.

## 2026-07-14 — Trim pass 2: remove Role/Date, Workspace = Members only
- **What worked:** Akram trimmed again — cut Role + Date format from My settings and reduced Workspace to only Members & Access. When a whole block goes, I deleted its functions AND its i18n-driven UI AND its event wiring in one pass, then inverted the guarding tests to assert ABSENCE. Kept a clean seam: the activity *page* route stayed (something else may link to it) while only its Settings shortcut button was removed — removing a shortcut ≠ removing the destination.
- **What nearly broke / the gotcha:** `verify-settings-layout` was almost entirely about the tier editor + Slack list — two features now gone. Rather than delete the suite, I repurposed it to assert the Workspace tab is now Members-only (tiers/slack/roles/activity-button all absent), so it still earns its place as a regression guard for the trim. A test whose subject was removed should be re-pointed at the new invariant, not silently dropped — otherwise you lose the guard that the old thing stays gone.
- **What I'd do differently next time:** Ask up front "does the destination stay, or just the entry point?" — I nearly removed the whole activity route when only the button was meant to go. Entry-point vs destination is a distinct decision; confirm it before cutting.
- **Rule to remember:** Removing UI = delete code + strings + wiring + invert the guarding test (assert it's gone), and re-point (don't delete) any suite whose subject was removed so the removal stays enforced. Distinguish "remove the shortcut" from "remove the feature."

## 2026-07-14 — Trim My settings (Akram feedback: less is more)
- **What worked:** Akram reviewed the shipped Settings and called out a lot as noise (density, date-format picker, quiet hours, digest, in-app channel, last-sign-in, device row, sign-out-everywhere, the whole Privacy catalogue + export). Rather than defend the "complete" version, cut hard to what a person actually uses: theme+language, three notification topics × two channels, change-password, one privacy line. Removed the entire `WP.privacy` module + its test instead of leaving dead code behind a hidden feature. Kept the *plumbing* that others depend on (density default still applied, `WP.fmt.date` still formats) — only the UI controls went. Answered "what does role mean?" with a per-level one-liner instead of the raw enum.
- **What nearly broke / the gotcha:** Two shipped test suites (`verify-security`, `verify-settings-v2`) asserted the presence of the very things being removed — so the cut *had* to come with rewriting them to assert ABSENCE (`!/sec-signout-all/`, `!/data-seg="density"/`). A removal isn't done until the test that guarded the old behaviour is inverted; otherwise the suite fails and you can't tell a regression from an intended cut.
- **What I'd do differently next time:** When a feature ships "complete," expect the real signal after someone uses it — build the first version lean enough that trimming is cheap, and don't over-invest in sub-features (quiet hours, digest, export) before there's a pull for them. The audit-driven "add everything useful" instinct fights the Constitution's Simplicity article; bias to the smallest thing that answers the job.
- **Rule to remember:** Ship the lean version and let use tell you what to add — removing shipped UI costs test rewrites + a deploy, adding later is one PR. When you remove UI, invert its guarding tests (assert it's GONE) and delete any module that only existed for it. Simpler wins absent evidence (Constitution, Simplicity).

## 2026-07-14 — Settings → Privacy (what Tempo tracks about me + export)
- **What worked:** Grounded "what we track" in what the code ACTUALLY stores (SAVE_KEYS/prefs, the append-only events store with per-event `source`, the activity log, the directory record) instead of writing marketing copy — every category on screen points at a real store, so it can't drift into a lie. The no-surveillance stance is enforced by construction: the events store already throws on an event with no `source`, and the privacy catalogue is a fixed list with no keystroke/presence/location/screen category — the test asserts none can be added. Export is a client-side Blob download (no server, no new endpoint) and `buildExport(data, now)` takes the timestamp as an argument so it's testable and free of a buried `Date.now()`.
- **What nearly broke / the gotcha:** My first test "no foreign events" check was vacuous — `myData()` strips `subjectId` from each event for the owner-facing shape, so filtering the *output* by `subjectId` always found nothing regardless of correctness. Fixed by asserting against the STORE (`WP.events.query(self)`) and checking `myData` returns exactly that count — a real guard, not a tautology. Also: a data-export must gather at the SELF layer (filter before you strip identifying fields), never trust the post-shaping output to prove scoping.
- **What I'd do differently next time:** When testing a filter, assert on something the filter could actually get wrong — if the shape you assert on has already discarded the field the filter keys on, the test proves nothing. Check the boundary (store → my-data), not the already-shaped tail.
- **Rule to remember:** For any "your data" surface, enumerate real stores (not aspirational categories), keep provenance (`source`) on every row, scope to self at the query layer, and make timestamps an injected argument. A privacy claim you can't point at a store for is a claim you shouldn't render.

## 2026-07-14 — Executive status entry point (deck link, role-gated)
- **What worked:** One gate helper (`WP.execDeckVisible()` = non-empty URL AND `WP.can('viewSettings')`) drives BOTH entry points, so the card and the nav item can never drift out of sync — a member can't see one without the other, and flipping the URL empty hides both at once. Kept the deck as a config-driven link that opens in a new tab (never embedded — it's a Google-auth surface). Reused the existing `external` icon and the `.acct-item` menu row (added only an `a.acct-item` text-decoration reset).
- **What nearly broke / the gotcha:** I first put the card inside `director()`, but the super-admin (`p_akram`) has `level === 'manager'`, so they render `leader()`, not `director()` — the card vanished for the very person who most needs it. The role *label* and the dashboard *variant* are different axes: `WP.can('viewSettings')` (capability) ≠ `viewer.level === 'director'` (org level). Fixed by rendering the card at the top of `render()` for anyone who passes `execDeckVisible()`, independent of which dashboard variant drew.
- **What I'd do differently next time:** When a feature is gated by a *capability*, place it where the capability is checked — not inside a branch keyed on org level/role name. Don't assume "director dashboard" == "everyone with director-level access."
- **Rule to remember:** Capability (`WP.can`) and org level (`viewer.level`) are separate axes; gate on the one that actually defines who should see the thing, and render it outside any role-variant branch. One gate helper for multi-placement features keeps every placement consistent.

## 2026-07-14 — Settings → Security (password · devices · sign-out-everywhere)
- **What worked:** Reused B1's `resetPasswordForEmail` for "change password" (email a secure link, never handle the old password client-side) and Supabase `signOut({scope:'global'})` for sign-out-everywhere. Was honest about what we can't do yet: no per-device session list (needs the sessions service) and 2FA marked "coming soon" — placeholders, not fake controls.
- **What nearly broke / the gotcha:** (1) The Security helpers first called `sbClient()`, which in jsdom injects the Supabase CDN `<script>` that never loads → the promise hung → the test's async IIFE never finished → Node exited 0 with NO output (a silently "passing" test). Fixed by using the already-live `WP._sb` and reporting offline immediately (also a real robustness win: a settings button should never hang on a CDN). (2) Reusing the `.mbr-row` class for the "this device" row broke `verify-members`' assertion that `mbr-row` isn't on the personal tab — gave it a distinct `.sec-device` class. (3) `sign-out-all` log entry doesn't survive because sign-out resets the in-memory activityLog — asserted via a `logEvent` spy instead.
- **What I'd do differently next time:** A test that exits 0 with NO PASS line is FAILING silently — always confirm the PASS string prints, not just the exit code. And don't call a CDN-injecting helper from a UI action; use the live client or fail fast.
- **Rule to remember:** UI actions must never hang on an external load — use the already-initialized client and fall back immediately. Reusing a CSS class couples unrelated features to each other's tests; give a distinct concern a distinct class.

## 2026-07-14 — Members & Access (allowlist becomes a screen)
- **What worked:** Read the access engine FIRST and found `grantAccess/listAccess/setAccess` already existed and already persisted (via `persist.js` `granted`), plus a whole `permissions.js` role screen. So the "big gap" was only surfacing the allowlist in Settings — I built the roster + grant/revoke toggle and *linked* to the existing role screen instead of rebuilding role-change logic. Less code, no duplication.
- **What nearly broke / the gotcha:** `grantAccess()` logs but does NOT persist on its own — the caller must `WP.setState({})` or the revoke is lost on reload. Also the roster must exclude `tbc` (open/unfilled roles) — they're not real members and would show fake toggles. And self-revoke had to be blocked or an admin could lock themselves out.
- **What I'd do differently next time:** Before building any "manage X" screen, grep for an existing engine/API (`grep -n "function .*[Gg]rant\|setAccess\|listAccess"`) — the capability often already exists headless and just needs a UI. Don't assume it must be built from scratch because it "lives in code today."
- **Rule to remember:** A mutation helper that logs is not the same as one that persists. After any `WP.access.*`/data mutation from the UI, call `WP.setState({})` so `persist.saveData()` runs. Reuse + link beats rebuild.

## 2026-07-14 — Settings v2 (personal / workspace split, preferences, notifications)
- **What worked:** Researched world-class settings IA first (Nicelydone / memorable.design / Equal.design) instead of guessing — confirmed the personal-vs-workspace split, "microcopy under every control", and the notifications model (channel × category + digest + quiet-hours). Reused existing components (`.seg`, `ui.subTabs`, `overlay-host`) rather than inventing new ones, so the new UI inherited house styling for free. Added `WP.prefs` with a `mergePrefs()` deep-merge so new pref keys never come back undefined on an old saved session.
- **What nearly broke / the gotcha:** Moving admin org-config under a "Workspace" tab silently broke `verify-settings-layout.js`, which rendered Settings and looked for tier/Slack markup that is now behind a non-default tab. Fix: the test sets `WP._settingsTab = 'workspace'` before asserting. Also the `__admin__` sentinel viewer has no `email`/`byId` row — the Account section had to guard (`WP.viewer()` + optional `roleLabel`) or it would throw.
- **What I'd do differently next time:** When splitting a page into tabs, grep the tests for anything that renders that page BEFORE running the suite — you can predict the break and fix the test in the same edit.
- **Rule to remember:** A preference control must actually DO something and PERSIST + confirm — `data-density` was inert until a CSS rule consumed it. Wire the effect, the persistence, and the saved-toast together, or it's a fake setting.

## 2026-07-19 — Feedback Polish → triage story + surface in Project delivery
- **What worked:** Grounding the build in the ACTUAL filter code first (`matchesType`/`matchesStatus` + `statusColorKey`) showed the existing Type/Status chips already had homes for triage states (Planned=violet/grey, Working=amber) — so "only filter in Project delivery" needed NO new chips or route, just a mapping. Reusing `data.items[]` as the merge point meant timeline + stats + filters all picked up feedback for free.
- **What nearly broke / the gotcha:** (1) Adding a second, NESTED fetch (feedback after exec) added several microtask hops — the exec test's 4 `await Promise.resolve()` no longer reached paint, so `doneChip` was undefined. Fixed with a `settle()` helper (16 ticks) and a URL-branching fetch mock. (2) `timelineItems` silently dropped `source`/`lane`/`wave` when copying `data.items` — feedback rendered as plain text with no tag until I threaded those fields through. (3) Classifier order matters: `500`/`fails`/`wrong` hit the Bug rule before Backend — correct behaviour, but my first test phrases were wrong, not the code.
- **What I'd do differently next time:** When a view gains a second async data source, update the test's settle-helper in the SAME edit as the fetch change — don't let stale tick-counts masquerade as a logic bug. And when mapping into a shared item shape, check the shape-consumer copies EVERY field you added.
- **Rule to remember:** Before inventing UI (new tab, new filter, new route), read the existing filter/bucket logic — the cheapest feature is one that rides rails already laid. And local-heuristic classifiers must be transparent + ordered most-specific-first, with the order pinned by tests.

## 2026-07-19 — Feedback triage controls (director-editable, local-overlay)
- **What worked:** A local-overlay store (localStorage) merged over the warehouse at fold time gave a fully working "director edits triage from the page" flow with ZERO backend — and it's warehouse-ready (swap load/save later, the overlay-merge stays). Structuring the store as pure fns on WP.fbTriage (load/set/apply) made it trivially testable.
- **What nearly broke / the gotcha:** The overlay applied at FOLD time (in load()), but Save repainted from the already-merged `lastData` — so a just-saved decision didn't re-apply and the chip stayed on the old status. Root cause: caching the DERIVED data and repainting from it, instead of caching the RAW inputs and re-deriving. Fix: keep `lastFeedbackRaw` + `lastBaseData`, and re-fold feedback (re-running feedbackAsItems, which re-reads the overlay) inside paintBody on every paint.
- **What I'd do differently next time:** When a render depends on mutable local state (an overlay, a filter), cache the RAW sources and re-derive on every paint — never cache the post-merge result and mutate around it. Also: jsdom's native localStorage only exists when the JSDOM `url` is set; a hand-rolled `w.localStorage={}` shim silently doesn't override it (my debug harness showed "undefined" while the real store worked) — trust the url-based native store, not a shim.
- **Rule to remember:** Derived-state caches are a trap. Cache inputs, re-derive on paint. And any per-row write control must re-read its source of truth on repaint, or the UI lies about what was saved.

## 2026-07-19 - Triage: suggestion engine + Save-feedback fix + panel polish
- What worked: reproducing the "Save does nothing" complaint in jsdom first proved the WRITE was fine - the real defect was (a) no visible confirmation so it FELT broken and (b) it only writes to a per-browser overlay. Fixed the felt-broken half (inline "Saved" tick that survives the repaint via rememberOpenTriage/restoreOpenTriage) and was honest about the shared-write half (an in-panel note + I commit real decisions to the warehouse myself).
- What nearly broke / the gotcha: the new "auto-prefill suggested wave on switch to Assigned" silently broke the existing "Assigned without a wave does not save" test - the item now HAD a suggested wave. Right call was to update the test to clear the wavesel explicitly (the guard still matters) + add suggestion assertions, not to weaken the guard.
- What I would do differently: I added klass/area/priority to the folded item AND had to remember to copy them through timelineItems (the same field-drop trap the skill already warns about). Next time, grep timelineItems the moment I add a feedback field.
- Rule to remember: when a user says "it is not saving", verify whether the write actually happened before assuming a write bug - often the write works and the FEEDBACK is missing, or the write target is wrong (local vs shared). Fix the felt experience and be honest about scope.

## 2026-07-19 - Merged a PR unlabelled -> it vanished from Project delivery
- What broke: PR #139 merged with NO wave: label. compute-exec-status.js only derives items from PRs carrying a wave:* label, so #139 never appeared on Project delivery - Akram asked "where is it?" and it genuinely was not there.
- Root cause: I ran the ship loop but skipped the label step; the labelled-PR-flow rule exists exactly for this and I did not follow it.
- Fix: gh pr edit 139 --add-label wave:exec-status, then gh workflow run exec-status.yml (workflow_dispatch) to re-derive now instead of waiting for the 7am cron, then let the chained Pages deploy publish. Confirmed live.
- Gotcha: there is NO type:* label - inferType reads the PR TITLE. "fix Save" in the title made it classify as Bug. Keep the title clean if you want the right type badge.
- Rule to remember: label the PR (wave:*) BEFORE or immediately AFTER merge, every time. If it is already merged and missing, add the label then trigger exec-status.yml manually - do not wait for the cron. Verify with curl of data/exec-status.json.

## 2026-07-19 - Seeding ideas as pre-triaged feedback items
- What worked: building the items in a Node script (not hand-JSON) guaranteed valid JSON + the exact append-only shape, and cross-checking each hand-triage against WP.fbTriage.suggest() proved my decisions were consistent, not arbitrary.
- Gotcha: pre-triaged items must carry status+wave+triagedAt/By/Note AND match the appender enums (klass in the allowed set) or a future real submission could look inconsistent next to them.
- Rule to remember: when seeding the warehouse, verify the fold renders (row count + wave-chip count + panel count in jsdom) before shipping - a bad field silently renders untagged.

## 2026-07-19 - Feedback image dropzone + stuck close-dialog fix
- What worked: refactoring onImagePick into a shared acceptFile(file) meant the input, drag-drop, and paste all reuse ONE validated path - no duplicated size/type/downscale logic.
- What nearly broke / the gotcha: the "Keep your feedback?" dialog was unclickable. The cause was NOT the dialog - it was the feedback panels OWN capturing keydown handler (Esc->requestClose, Tab->focus-trap) stealing the keys from a dialog that lives in a DIFFERENT host (#overlay-host). A focus-trap must yield when a modal opens ON TOP of it.
- Rule to remember: any component with a capturing key handler / focus-trap must detect a higher modal (query the shared overlay-host for .dlg) and STAND DOWN while it is open, or it silently traps the user. Test it by asserting the trap does NOT preventDefault(Tab) while the dialog is mounted.

## 2026-07-19 - A code merge did NOT auto-deploy (concurrency race with bot commits)
- What broke: PR #145 merged to main (9eae687) but the push-triggered Pages deploy never ran, so the code change was live-invisible until I dispatched deploy.yml by hand. Verified: no deploy run ever had headSha 9eae687.
- Root cause: deploy.yml uses concurrency { group: pages, cancel-in-progress: false }. Right after the merge, the exec-status + status-report BOT commits landed and queued their own workflow_run deploy. GitHub coalesces queued runs in a concurrency group - my merge push deploy was superseded/dropped by the newer bot-triggered deploy, which built the OLDER data SHA, not my code SHA.
- Fix used: gh workflow run deploy.yml (workflow_dispatch) after confirming the merge SHA was missing from the deploy run list. Then curl the live bundle for a marker (grep ex-tl-band) to PROVE the code shipped.
- Rule to remember: after merging a CODE PR, do not trust that push auto-deployed. Check that a deploy run exists for the exact merge SHA (gh run list ... | grep <sha>); if missing, dispatch deploy.yml manually and re-verify live by curl. The bot data-commits racing the merge is the trap. Consider: give data-deploys and code-deploys separate concurrency groups so they cannot cancel each other.

## 2026-07-19 - Adversarial self-review caught 4 real bugs in the days shipped work
- What worked: spinning up 3 parallel adversarial reviewers (one per feature) + my own live render pass found bugs that MY OWN "done" claims had missed - exactly why independent verification matters. A keyword-matcher (WAVE_OWNERS) that "looked fine" was over-matching on generic words.
- The 4 bugs: (1) suggestWave first-match-wins over-matched /delivery|status/ -> Slack idea suggested Wave 1 not 4; fixed by SCORING owners with strong(surface)/weak(generic) signals and weighting area over note. (2) Testing status folded to the wrong band vs its own comment; fixed the map+comment to be consistent (Testing = active = In progress). (3) chip() rendered raw English status in Arabic mode; added execChip_* i18n. (4) a single Escape re-entrantly opened+closed the confirm dialog because requestClose() mounts a dialog whose capturing keydown catches the SAME event; fixed with stopImmediatePropagation.
- Rule to remember: a regex/keyword matcher over free text WILL over-match generic words - score signals by specificity and weight the authoritative field (area) over free text, never first-match-wins on a broad alternation. And: synchronously mounting a new capturing key-listener inside a keydown handler re-enters on the same event; stopImmediatePropagation before mounting.

## 2026-07-19 - Native select leaked past the branding
- What broke: the table "Show N" pager select rendered as the raw OS widget (white box + native arrow) in a dark UI. The base rule at app.css:382 grouped select with .btn/input but only set transition - it never applied appearance:none or a surface look, so any UNCLASSED select fell back to the OS default.
- Fix: a branded select:not([multiple]) baseline - appearance:none + surface/token border/radius + a caret from two token-coloured gradients (no embedded hex -> passes token-purity, follows light/dark, flips in RTL). Classed selects (.fb-select, .ex-triage-f select) had to switch background shorthand -> background-color (shorthand was wiping the gradient caret) and reserve 30px caret padding.
- Gotcha: the `background: var(--x)` SHORTHAND silently resets background-image (my caret). Use background-color when a rule should keep an inherited background-image.
- Rule to remember: style native form controls (select especially) at the ELEMENT baseline, not only via opt-in classes - one unclassed instance leaks the OS widget and breaks the brand. Sweep ALL routes (getComputedStyle appearance!==none) to prove none leak.

## 2026-07-19 - "95% but everything is done" - abandoned PR leaked into progress
- What broke: the launcher showed 95% delivered while the counts said 24 shipped / 0 in progress / 0 need you. Effort-weighted progress = merged churn / TOTAL churn, and a closed-but-unmerged PR (#115) still had its change-size in the denominator - pinning progress below 100% forever even with everything real merged.
- Why I missed it earlier: I dropped that same ghost PR from the TIMELINE (display) but not from the MATH. A fix to how something renders is not a fix to how it is computed - check every consumer of the excluded data.
- Fix: filter closedUnmerged PRs up front so they count in NO metric (progress, health, counts). Extracted waveProgress() as a pure fn with unit checks in --selftest, incl. the exact regression (all-merged wave = 100% even with a heavy-churn closed-unmerged ghost).
- Rule to remember: when you exclude data from ONE view, grep every place that reads that data - a display filter and a math filter are different fixes. And headline metrics must reconcile with their own sub-counts (100% <=> 0 remaining); if they disagree, one formula is wrong.

## 2026-07-19 - Waves batch: focus, completion-notify, strategic backlog
- What worked: breaking a big multi-part request (per-wave bars, notify Ahmed, BCG ideas) into 3 focused PRs kept each verifiable + shippable instead of one risky mega-change.
- What nearly broke: switching notifications.js from the old JSONP endpoint to fetch() broke a jsdom test that had no fetch (ReferenceError). Two-part fix: guard the code (typeof fetch !== function -> resolve empty) AND stub fetch in the test. Lesson below.
- Gotcha: a shared cache (notif execWaves) means a later test re-open does NOT re-fetch; the empty-state test had to seat a MEMBER (structurally empty) rather than rely on a new fetch stub taking effect.
- Also: found the notif panel was STILL on the dead Google/JSONP path post-migration - a stale consumer of exec data. When you migrate a data source, grep every reader, not just the main view.
- Rule to remember: any code that calls a browser global (fetch) must degrade if it is absent (older/headless envs + tests), and migrating a data source means sweeping ALL its consumers.

## 2026-07-20 — Timeline hide-Delivered + wave-assign + two feedback bugs
- What worked: collapsing the Delivered band as a toggle button (default closed, one click away) reads far cleaner than a 32-item finished wall; guarding collapsibility with filterType/filterWave/filterStatus === default so any active filter keeps rows visible; a live Playwright hit-test (elementFromPoint on the OK button) proved the dialog was actually clickable, not just present.
- What nearly broke / the gotcha: (1) the "Keep your feedback?" dialog was unclickable because .overlay was z-index 40 while .fb-overlay is 1000 - the feedback backdrop sat on top and ate every click, and each click re-fired requestClose (a loop). Fixed by raising .overlay to 1100 (above the panel 1001, below toasts 1200). (2) showDelivered is a module-scoped closure shared across renders, so a test that blindly clicked the toggle flipped an already-open band shut - always check aria-expanded before toggling in tests. (3) stale dist: edited src but an early test run loaded the old build and reported wrong counts - always rebuild before trusting a jsdom count.
- What I'd do differently next time: run the live hit-test FIRST for any "I can't click X" report - z-index stacking bugs are invisible to DOM-presence asserts and to screenshots.
- Rule to remember: any modal that can open ON TOP of the feedback panel must sit above z-index 1001; "the element renders" is not "the element is clickable" - hit-test it.

## 2026-07-20 — Saved feedback appeared nowhere (purpose vs mechanism)
- Root-cause CLASS: `write-and-read-to-different-stores`. "Save feedback" wrote to localStorage['tempo_feedback_saved']; Project delivery read feedback ONLY from data/feedback.json. The two never met, so a saved item was a dead write, visible nowhere.
- Why it escaped: I verified the MECHANISM (a record landed in localStorage) and declared done. I never verified the PURPOSE the feature exists for (the user can SEE their saved feedback on the page). Akram had to find it in production.
- Fix + class sweep: exec.js now folds WP.ui.feedback.savedItems() into loadFeedback() (de-duped by id, newest first); saveLocally() stamps a valid id + status:'New' so feedbackAsItems can render it; a save while on Project delivery triggers WP.render() so it appears immediately. Grepped for other write/read store splits - triage overlay already shares its store correctly; no other instance found.
- Standing check added: verify-exec.js now asserts a localStorage-saved feedback item SURFACES on the timeline even when the warehouse lacks it. Plus a Playwright purpose-test (save -> assert the unique text appears on the delivery page).
- Rule to remember: prove the PURPOSE, not the mechanism. For any "save/create/submit X", the done-check is "X is now visible where the user expects it", never "X was written to storage". If a feature writes data, a test must READ it back through the real UI path.

## 2026-07-20 — Adversarial self-review found what my normal review missed
- Method: instead of confirming my own work, I ran 4 parallel probes each told to BREAK a specific area (data-flow, timeline, dialog stacking, live behavior), then re-verified every finding against the real artifact before trusting it.
- Real bugs caught (that my "it works" pass missed): (1) in All view one Delivered toggle expanded EVERY week's band - a single module flag shared across per-week bands; (2) a week with only Done items collapsed to a lonely header with no rows = looks broken; (3) on localStorage quota, saveLocally's catch{} swallowed the error while submit() cleared the draft + toasted "Saved" = silent data loss; (4) RTL caret never flipped.
- Equally important: I DISPROVED 3 confident-looking findings by re-testing - the live probe's "only first save persists / silent data loss" was a FALSE POSITIVE from its own flaky selectors (proven: 3/3 saves persist and show live); the id-collision was not reproducible; XSS was already safe. A finding is a hypothesis until re-verified on the real thing.
- Root-cause classes: `shared-flag-across-repeated-instances` (fix: key state per instance, not one bool), `silent-catch-then-claim-success` (fix: return a success bool + caller keeps work + honest error), `looks-empty-when-all-collapsed` (fix: explicit hint line).
- Rule to remember: adversarial > confirmatory. Spawn independent breakers, give each a distinct failure class, and RE-VERIFY every finding (both directions: real bugs AND false alarms) before acting. Never trust a single probe's verdict - including my own.

## 2026-07-20 — Show feedback screenshots on Project delivery
- What worked: feedback already CAPTURED an image (dataUrl, <=5MB, downscaled); the gap was purely on the read side. Wired the local-save path to carry the image and rendered a clickable thumbnail + lightbox on the delivery timeline.
- What nearly broke / the gotcha: the image was dropped by a SILENT FIELD WHITELIST. `timelineItems()` rebuilds every item object field-by-field (id, title, status, ...) and simply omitted `image` - so even though `feedbackAsItems()` correctly carried it, the row that reached `tlRow()` had no image. I only found it by probing at 3 points (feedbackAsItems entry = IMG present, tlRow entry = img gone) and bisecting the two whitelisting maps between them. Two SEPARATE reconstructions (feedbackAsItems AND timelineItems) each had to be updated. Same class as the earlier "write-and-read-to-different-stores" escape.
- Also: the pending-composer note built at submit() time was a DIFFERENT object shape than a queued item and omitted image too - a third place the field leaked out. Three drop points for one field.
- Verification trap: `file://` blocks fetch() in Chromium, so a Playwright pass on dist/index.html shows an EMPTY timeline (no data loads) and silently "passes" nothing. Had to serve dist + data over `python3 -m http.server` to get a real render. jsdom (stubbed fetch) is the reliable functional gate; HTTP-served Playwright is the reliable visual gate. file:// is neither.
- Rule to remember: when a field "disappears" between capture and render, suspect a field-whitelist reconstruction, not a logic bug - and grep for EVERY `.map(function(it){ return { ...explicit fields... } })` on the path, because one object can be rebuilt several times. Add the field to all of them + a test that asserts it survives end to end.

## 2026-07-21 - Local system server (run Tempo on your machine, save feedback+images to disk)
- What worked: a tiny pure-Node http server (tools/local-server.js, zero deps) serves the built app AND accepts POST /api/feedback, decoding the inline data-URL image into a REAL file under data/feedback-images/ and rewriting the record's `image` to a served path. The Project delivery page then shows it exactly like the live path - no UI change. The client seam is a single config key (feedbackLocalEndpoint) that auto-turns-on when the page is served from localhost, so GitHub Pages behaviour is untouched (falls back to per-browser save).
- What nearly broke / the gotcha: images must NEVER be committed - added data/feedback-images/ + data/feedback.json.tmp to .gitignore, and the e2e writes into the real data/ so I had to `git checkout data/feedback.json` + rm the images dir after verifying, or the working tree carries local test data.
- The other gotcha: the confirmation toast said "on this device" (localStorage wording) which is now untrue for the local server (it's on disk, one shared file). Added fbSavedDiskN* strings so the message tells the truth ("saved to your local system").
- What I'd do differently: expose an explicit WP.ui.exec.refresh() so the feedback module doesn't have to lean on WP.render() re-fetching; today it works because the exec load is cache-busted no-store on every render.
- Rule to remember: the local server is the EXACT seam we swap for Supabase at go-live - client POSTs feedback to an endpoint; only the endpoint URL + storage behind it change. Keep that boundary clean so "switch to server" stays a config change, not a rewrite.

## 2026-07-21 - Un-defer the advanced layer + make wave % reflect real done/planned
- What worked: the whole advanced surface set (Library, Weekly, Wellbeing, Fairness, Upward, Org, Eval prep/band/consistency, Timeline) was HIDDEN behind one flag (WP.config.mvp), never deleted - flipping it to false brought back working, wired pages (nav + routes + panels) in one line. And a live deriver (deriveWaveProgress) now computes each wave % = done/planned from the items themselves, plus the headline = total done/total planned, so the numbers are honest and move as we ship (35/44 = 80% today).
- What nearly broke / the gotcha: (1) verify-mvp-flag asserted the OLD default (mvp=true) at load time, BEFORE the test set the flag - had to make the default-value asserts explicit for both states. (2) Planned backlog items were invisible in the timeline until they got a `ts`: the timeline only renders DATED items, and undated ones fall into an "Undated" group that weekly view hides. Stamping ts=today put them in the current week's "To decide" band when you focus their wave. (3) cover.progress in the JSON is now IGNORED and derived - a stale asserted number would silently mislead, so the deriver overrides it whenever any wave has a plan.
- What I'd do differently: seed each wave's planned count + items in a durable backlog file (data/delivery-backlog.json) rather than editing exec-status.json directly, since a CI refresh can overwrite exec-status.json (see wbk-delivery-durable-backlog pattern). Flagged as follow-up.
- Rule to remember: when a % should be "truthful", DERIVE it from the underlying items at render time and treat any stored number as a fallback only - never trust an asserted percentage that can drift from the data behind it.

## 2026-07-22 - Home cleanup + reminder redesign + FAB/footer collision + sidebar trim
- What worked: five small, independent UI fixes in one pass - removed the Project-delivery dashboard card (+ dead .exec-card CSS), redesigned the eval reminder bar (icon chip + two-line title/sub + trailing CTA, calmer than the old flat strip), fixed the Feedback FAB covering the footer credit name, and dropped the "Components" (design-system showcase) from the sidebar (route kept). A full Playwright QA matrix (dark/light x en/ar x director/peer) proved each.
- What nearly broke / the gotcha: (1) the FAB-vs-footer bounding-box test reported "overlap:true" for a peer even after the fix - because the FAB geometrically sits INSIDE the footer's reserved right-padding zone. The real check is "is the TEXT under the FAB", not "do the boxes touch"; the last credit name ended at x=1308 while the FAB started at x=1364 -> no actual overlap. Geometry-intersection != visual collision. (2) removing 'library' from the nav list broke verify-mvp-flag, which asserted it RETURNS to nav when un-deferred - had to split "is a deferred ROUTE" (still true) from "is a NAV entry" (now false).
- What I'd do differently: when a request bundles several unrelated UI tweaks, still ship them together (one Home pass) but write the QA harness FIRST as a matrix so each fix is proven in every theme/lang/role, not spot-checked.
- Rule to remember: to prove a floating element doesn't cover content, assert on the CONTENT rect vs the floater rect, never on the container rect - reserved padding makes container-intersection a false positive.

## 2026-07-22 - QA caught the CI wiping planned wave items (honest % regressed to 100% live)
- What worked: doing QA against the LIVE deployed site (not just local) caught a real regression the local pass could never see - the live headline read 100% instead of the honest 80%. Root cause: the github-actions "exec-status update" job regenerates data/exec-status.json from merged PRs right after every push, and it wiped the planned backlog items + wave `planned` counts I'd hand-added to that file. My deriveWaveProgress code shipped fine; its INPUT data got clobbered, so it fell back to the stale stored cover.progress=100.
- What nearly broke / the gotcha: this is the exact follow-up I'd flagged and deferred ("seed planned items in a durable file"). Deferring it meant it shipped broken for a day. The fix is the durable-backlog pattern (same as WBK): data/delivery-backlog.json holds planned items + wavePlans + defaultDeliveryWave, folded into the timeline + % at RENDER time (like feedback), so CI regenerating exec-status.json can't touch it. Also had to teach the deriver that CI items carry NO wave field -> default them to wave 1 via the backlog's defaultDeliveryWave.
- Test gotcha: adding loadBacklog() as a second best-effort fetch broke verify-exec because the jsdom fetch stub routed the backlog URL into the exec-status branch (polluting fetchedUrl + returning the exec payload as the backlog). Fixed by giving the stub a dedicated delivery-backlog.json branch (null => 404 by default so old tests are unaffected).
- Rule to remember: if a data file is REGENERATED by CI, never hand-edit it - any durable additions MUST live in a separate file folded at render time. And ALWAYS QA the deployed site after a data change, because a post-merge CI job can silently rewrite your data even when code + local pass.

# Tempo — Delivery Log (for BA → Excel → Director)

Every finished item is logged here in a **paste-ready** row so the BA can drop it into the
tracking sheet and the Director can see what shipped, whether it was a **Feature** or a **Bug
fix**, and where we are. Newest at the top.

**Column key:** Date · Type (Feature / Bug fix / Improvement) · Title · What changed (plain) ·
Value (why it matters) · Status · PR · Live?

| Date | Type | Title | What changed | Value | Status | PR | Live |
|------|------|-------|--------------|-------|--------|----|------|
| 2026-07-14 | Change (security posture) | Auth reverted to directory stopgap | Flipped `WP.config.authMode` from `password` back to `directory` so a registered @webook.com email signs in immediately with NO password/external account (unblocks sign-in while no Supabase Auth users exist). **NOT identity-proof** — anyone who types a known @webook.com email gets in. | Unblocks the director now on an obscure URL. **Rollback:** change the one word `directory`→`password` once real accounts exist. | Shipped | #97 | ✅ |
| 2026-07-14 | Improvement | Executive Status chip sizing + alignment | Status chips are now a uniform fixed-width pill (96px, centred) and priority is a fixed right-aligned column, so chips + HIGH/MEDIUM/LOW form clean vertical columns in both themes (were ragged, each sized to its label). | Cleaner, more scannable requests list; nothing jumps. From Akram's UI review. | Shipped | #99 | ✅ |
| 2026-07-14 | Improvement | Executive Status UX + colour + order | Reordered the view so "What needs you" sits right after the cover (was last) — the ask blocked on the director is no longer buried. Tightened status-chip colours to pass WCAG AA in light + dark (amber text was muddy); added dark-mode exec colour tokens so chips/bar read on dark. | The director scans top-down and sees what needs them first; every status colour is legible and trustworthy. From Akram's product review. | Shipped | (pending) | ⏳ |
| 2026-07-14 | Change | Org moves (Sports/Motaa team) | Moved Shamma Alsagr to report under A. Maksosah (was under Motaa directly); added a new Event Operations Specialist "Saleh" reporting to Khaled Jeneina. | Org tree matches the real reporting lines Akram specified. | Shipped | (pending) | ⏳ |
| 2026-07-14 | Feature | Executive Status native in Tempo | A Director/Admin-only "Executive status" nav item + dashboard card open a NATIVE in-app view that reads the live "Tempo - Feedback (Live)" sheet through a deployed Apps Script JSON endpoint (JSONP) and renders on-brand: cover rollup + % delivered bar, your requests, one card per wave, and "what needs you" (derived from your open requests). A "Open / present" button opens the Google Slides deck (kept PRIVATE) for board/PDF/present mode. Sheet is the single source of truth; deck + page are two thin views over it, neither stores data, so neither drifts. | The director sees the current status natively inside Tempo (no Google sign-in wall, works for anyone with a Tempo role, real-time), and still has the polished deck one click away for the boardroom. | Shipped | #95 | ⏳ |
| 2026-07-14 | Feature | Executive Status Deck (Google Slides) | Delivered a Google Apps Script that turns the Feedback sheet into a self-updating, WBK-branded Slides deck (cover + your-requests + one slide per wave + "what needs you"). Director-only link, rebuilds on edit + daily. Lives entirely in Google — not in the app. | The director gets one private link that always answers "what's the status?" — no app, no maintenance, updates itself from the sheet. | Delivered (paste Code.gs + run setup) | (Google) | n/a |
| 2026-07-14 | Reverted | In-app Executive Status page | The earlier in-app exec page (#91) misread the ask; the deck is a standalone Google Slides artifact, not an app page. Removed the page/nav tab/card. | — | Reverted | #93 | ✅ |
| 2026-07-14 | Improvement | Full-width (fluid) layout | Every page now fills the whole screen instead of sitting in a narrow centred column with a big empty area on the right. | Uses the whole screen; no wasted space; tables, lists and cards get room to breathe. | Shipped | #92 | ⏳ |
| 2026-07-14 | Feature | Executive Status page (live) | Built the Executive Status "deck" as a real, self-updating page inside Tempo: portfolio health, who needs your attention now, growth signals, and team-by-team load. Reached from a nav tab + a dashboard card (Director/Admin only). Replaces the earlier idea of a separate Google Slides link. | The director gets a live one-screen status that's always current — no slides to maintain, no external link. | Shipped | #91 | ⏳ |
| 2026-07-14 | Bug fix | Override log restricted to Super Admin | The Activity & override log was visible to directors and all admins; now it shows to the Super Admin only. | Sensitive who-did-what provenance is no longer exposed to directors/other admins. | Shipped | #90 | ✅ |
| 2026-07-14 | Improvement | Settings simplified (round 2) | Removed Role and Date format from My settings. Reduced the admin Workspace tab to just "Members & Access" (removed tier weights, capacity-state reference, the roles reference wall, the activity-log button, and Slack linking). Members roster dropped the role chip and the "manage roles" link. | Even leaner and clearer; the admin area is now one focused job — who can sign in. From Akram's feedback. | Shipped | #88 | ⏳ |
| 2026-07-14 | Improvement | Settings simplified (Akram review) | Trimmed My settings to what people actually use: role now explains itself in plain words; removed density, date-format picker, quiet hours, digest, in-app channel, last-sign-in, device list, sign-out-everywhere, and the privacy catalogue + data export. Notifications is now two simple questions (what / where); Security is just "change my password"; Privacy is one reassurance line. | Easier and faster to read; no clutter. Directly from Akram's feedback. | Shipped | #87 | ⏳ |
| 2026-07-14 | Feature (superseded) | Settings → Privacy (export) | Detailed "what Tempo tracks" catalogue + "export my data" (#86) — **removed in #87** as unnecessary; replaced by a single one-line privacy reassurance. | — | Reverted | #86→#87 | ➖ |
| 2026-07-14 | Feature | Settings → Privacy | A plain-language list of exactly what Tempo keeps about you (with a why for each), a clear "what Tempo never does" statement (no keystroke/presence/location tracking), and one-click "Download my data" (only your own). | Builds trust — people see the no-surveillance promise made concrete, and can take a copy of their data. | Shipped | #86 | ⏳ |
| 2026-07-14 | Feature | Executive status (deck link) | A one-click "Executive status" entry point (dashboard card + account-menu item) that opens the live Executive Status Deck in a new tab. Director + Admin only; hidden until the deck link is set. | The director reaches the live portfolio deck instantly from inside Tempo, without hunting for a link. | Shipped (awaiting deck link) | #85 | ⏳ |
| 2026-07-14 | Feature | Settings → Security | Change your password (secure emailed link — we never see it), see your last sign-in, and "sign out everywhere" (all devices at once). 2FA marked coming-soon. | Users control their own account security; a lost/shared device can be cut off instantly. | Shipped | #84 | ✅ |
| 2026-07-14 | Feature | Settings → Members & Access | Manage who can sign in (the access allowlist) + see each person's role, from the UI. Was editable only in code before. | Admins control access without a developer; every change is logged. | Shipped | #82 | ✅ |
| 2026-07-14 | Feature | Settings v2 (personal + workspace) | Split Settings into "My settings" (account, appearance, language, density, date format, notifications) for everyone, and "Workspace" (org config) for admins. | Every user can manage themselves; admin config no longer clutters everyone's view. | Shipped | #81 | ✅ |
| 2026-07-13 | Improvement | QA/UX-UI audit fixes | Reusable in-app dialogs (no more raw browser popups), WCAG contrast fixes, explainable burnout flag, mobile sign-in button, Arabic greeting. | Cleaner, more accessible, more professional across the app. | Shipped | #80 | ✅ |
| 2026-07-13 | Bug fix | Password-only sign-in | Sign-in is now password-only; a leftover Google session can no longer let someone back in. | Closes an access-control hole; only people with a password get in. | Shipped | #79 | ✅ |
| 2026-07-13 | Bug fix | Audit safe fixes | Fixed two crash cases (profile, assignment), rating-scale accessibility, empty states, undefined color tokens, mobile popovers. | Fewer crashes, more accessible, tidier UI. | Shipped | #78 | ✅ |
| 2026-07-13 | Bug fix | Account button too small to tap | Enlarged the top-right account control to a proper 44px tap target. | Fixes a "can't click it" complaint on smaller screens. | Shipped | #77 | ✅ |
| 2026-07-13 | Bug fix | Settings dead space | Removed the wasted empty strip on the Settings page (grid layout fix). | Page uses the full width; looks finished. | Shipped | #76 | ✅ |

## Open / next (roadmap — not yet built)
| Type | Title | Note |
|------|-------|------|
| Feature | Settings → Security | Change my password + active sessions + "sign out everywhere". Pairs with B1's password work. |
| Feature | Settings → Privacy | "What Tempo tracks about me" + export my data. |
| Feature | Settings → Help/About | App version, keyboard shortcuts, support/contact. |
| Feature | Notification delivery | Notification preferences are saved but not yet acted on — wire real email/Slack/in-app sending. |
| 2026-07-19 | Feature | Polish turns feedback into a clear, labelled story | The "Polish" button now rewrites a user's note into a tidy story (what, why it matters, what to do) AND labels what it's about — front-end, back-end, a bug, a new feature or skill — and sets the Type automatically. Works with no AI backend. | People write messy one-liners; this makes every piece of feedback readable and pre-sorted so the team can act on it fast. | Shipped | #TBD | pending |
| 2026-07-19 | Feature | Feedback shows up in Project delivery with a lifecycle | Submitted feedback now appears on the Project delivery timeline with a status it moves through — New → Testing → Under review → Assigned to a wave, or Discarded — and can be found with the existing Type/Status filters. | The director can review incoming ideas in one place, decide if each is worth it, assign it to a delivery wave, or drop it — with a full audit trail. | Shipped | #TBD | pending |
| 2026-07-19 | Feature | Director can triage feedback from the page | Each feedback item on Project delivery now has a small gear control: the director can set its status (New / Testing / Under review / Assigned / Discarded), assign it to a delivery wave, or discard it - and the row updates instantly. Decisions are saved in the browser for now (a shared save needs the same backend as Send). | Turns the read-only feedback board into a real triage tool: the director decides what's worth doing, places it in a wave, or drops it - without editing any files. | Shipped | #TBD | pending |

| 2026-07-19 | Improvement | Smart triage: suggested Status + Wave, clearer Save, cleaner panel | When a director opens a feedback item, the panel now recommends where it should go and why (e.g. "a bug on the weekly report -> Wave 1"), pre-fills that choice, and shows a clear "Saved" tick when they confirm. The panel was redesigned to be easier to read, with a Cancel button and an honest note about where the decision is stored. | Turns triage from manual guesswork into one-click confirmation and removes the "did it even save?" confusion. | Shipped | wave/triage-suggest-polish | akram-webook.github.io/tempo |

| 2026-07-19 | Feature | Seeded product backlog into Project delivery | Added 9 ranked product ideas (features, improvements, suggestions) as pre-triaged items so they appear on the delivery page: shareable digest, burnout early-warning, Slack post, wave trend arrows (all Assigned to a wave), plus 5 valid ideas parked under Review. | Gives the team a live, prioritized backlog in the one place everything now lives - no side sheet. | Shipped | #141 | akram-webook.github.io/tempo |

| 2026-07-19 | Improvement | Drag-and-drop image upload in feedback + fixed stuck close dialog | The "Add image" control is now a dropzone - drag an image, click to choose, or paste it. Also fixed a bug where the "Keep your feedback?" popup could not be closed. | Faster, more natural way to attach a screenshot; and users are no longer trapped by the close popup. | Shipped | #143 | akram-webook.github.io/tempo |

| 2026-07-19 | Improvement | Ordered the Project delivery timeline into clear sections | The timeline used to be a flat list of everything mixed together. It now groups items into labelled sections - Delivered, In progress, To decide, Discarded - with a count each, and orders them by wave then newest, so it reads top to bottom. | Much easier to scan "what is shipped vs what still needs a decision" at a glance. | Shipped | #145 | akram-webook.github.io/tempo |

| 2026-07-19 | Bug fix | Review pass: fixed 4 issues in the day (wave suggestions, Testing grouping, Arabic labels, close popup) | The wave a feedback idea is suggested for is now accurate (a Slack idea no longer suggested to the wrong wave); items being tested group under In progress; status labels show in Arabic; and the "Keep your feedback?" popup opens reliably. | Keeps the new triage + timeline trustworthy and fully bilingual. | Shipped | #148 | akram-webook.github.io/tempo |

| 2026-07-19 | Improvement | Branded every dropdown to match the design system | Dropdowns like the "Show 10" page-size picker were showing the plain browser style instead of the app style. All dropdowns across the app now match - same surface, border, and arrow - in light, dark, English and Arabic. | Consistent, professional look; no jarring native controls. | Shipped | #150 | akram-webook.github.io/tempo |

| 2026-07-19 | Bug fix | Delivery % now reads 100% when everything is shipped | The headline showed 95% even though every item was delivered and nothing was in progress. An abandoned (closed, never-merged) pull request was still being counted against progress; it no longer is. | The top-line number is now trustworthy and matches the shipped/in-progress counts. | Shipped | #152 | akram-webook.github.io/tempo |

| 2026-07-19 | Feature | Focus a wave by clicking it (per-wave bars + jump) | Each wave now has its own progress bar; click a wave to see only its items, click again to show all. Done waves stay visible. | Lets the team focus on one wave at a time instead of one long mixed list. | Shipped | #154 | akram-webook.github.io/tempo |
| 2026-07-19 | Feature | Director is notified when a wave is 100% done | When a wave reaches 100%, the director (Ahmed) gets an in-app alert that it is complete, linking to the delivery page. | Ahmed knows the moment something is finished without checking. | Shipped | #155 | akram-webook.github.io/tempo |
| 2026-07-19 | Improvement | Strategic backlog mapped to waves | Added 7 senior-PM/strategy ideas, each assigned to the wave it serves (or parked with a reason), and closed one that shipped. | Gives each wave a real, prioritized backlog to focus on. | Shipped | #156 | akram-webook.github.io/tempo |

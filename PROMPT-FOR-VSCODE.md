# Paste-into-VS-Code prompt (Claude Code / Cursor)

Copy everything in the block below and paste it as your first message to the AI in VS Code.

---

You are working on **Tempo**, an internal Workforce-Intelligence / Operations web app for
Webook Event Operations. Read this whole brief, then do the setup tasks at the end.

## IMPORTANT — read first: repo state vs. the real source
The GitHub repo currently contains only the **built, bundled output** (`index.html`, a ~400 KB
monolith) plus `Tempo-Review.html` and a README. The full **modular source** (the `src/` tree,
`build.js`, `test/`, `package.json`, `.github/workflows/deploy.yml`, docs) was developed separately
and is being **added to the repo from a provided zip (`tempo-source.zip`)**. So:
- After the zip is unpacked into the repo, `index.html` at the root becomes a **generated artifact**
  (the output of `build.js`), and `src/**` is the real source you edit.
- If you open the repo and only see the monolith, the source hasn't been committed yet — STOP and
  ask for `tempo-source.zip` (or unpack it) before doing anything else. Do NOT hand-refactor the
  monolith from scratch; the modular source already exists.

## Project facts
- Repo (source of truth once the zip is committed): https://github.com/akram-webook/tempo  (branch `main`)
- Live site: https://akram-webook.github.io/tempo/  (GitHub Pages serves the bundled `index.html`)
- Stack: **plain vanilla HTML/CSS/JS, no framework, no required build tooling.** Everything hangs
  off a single global namespace `window.WP`. The app must keep working by simply opening
  `index.html` in a browser.
- Languages: English + Arabic (RTL/LTR). Light/dark themes. Design tokens are WBK
  (brand pink `#ff2c79`).

## How the code is organized
- `index.html` — the shell + the ordered `<script>` tags. **Load order matters:** data → core → ui → app.
- `src/css/tokens.css`, `src/css/app.css` — tokens + all component styles.
- `src/js/data/*` — sample data: `mock-data.js` holds PEOPLE, teams, the email directory `EMAILS`,
  and the `superAdmin` flag (akram@webook.com).
- `src/js/core/*` — `config.js` (sign-in config), `i18n.js` (EN/AR strings), `state.js`
  (`WP.state` + `WP.setState` + `WP.viewer`), `access.js` (roles: canManage/hasAccess/isSuperAdmin),
  `capacity.js`, `growth.js`, `persist.js` (saves the user's work to localStorage).
- `src/js/ui/*` — views; each renders into `<div id="view">`. `workloadMap.js` is the largest.
  `login.js` is the sign-in (verified-link via Supabase / Google / directory-gate fallback).
- `src/js/app.js` — boot + shell + routing (`WP.state.route`) + the render loop.
- `build.js` — bundler: inlines all `src` CSS/JS + base64 SVG logos into one `dist/index.html`.
- `dist/index.html` — the GENERATED, deployable single file.
- `test/*` — headless jsdom suites (`verify-all.js`, `verify-login.js`, etc.).

## Architecture model (follow it)
- State is the single source of truth in `WP.state`. Views never hand-mutate the DOM globally —
  they call `WP.setState(patch)`, which persists and calls `WP.render()`.
- New strings go in `src/js/core/i18n.js` as `{ en, ar }` and are read via `WP.i18n.t('key')`.
- Icons are inline SVG from `src/js/ui/icons.js` — never use emojis as icons.

## GOLDEN RULES (do not break)
1. **Edit files in `src/` only. Never hand-edit `dist/index.html` — it is generated by `build.js`.**
2. After changing `src/**`, run `node build.js`, then run the jsdom tests; both must pass before commit.
3. **Never commit secrets.** The Supabase URL + publishable (`sb_publishable_…`) key in
   `src/js/core/config.js` are public by design and fine to keep. NEVER add the Supabase
   `service_role` key or any private key to the front-end or the repo.
4. Keep the verified sign-in working: `login.js` + `WP.auth.initSession()` (called in `app.js`)
   consume the magic-link session and map the email to a registered person. Don't regress that.
5. Use a branch + PR per task. Don't push broken builds to `main`.

## Auth (current state, already live)
- Verified sign-in is ON. User enters their `@webook.com` email → Supabase emails a one-time
  sign-in link → opening it signs them in (email must match the `EMAILS` directory).
- Supabase dashboard is already configured: Site URL `https://akram-webook.github.io/tempo/` and
  redirect allowlist `https://akram-webook.github.io/tempo/**`. For local testing add
  `http://localhost:3000/**` (or your local port) to that allowlist.

## The repo already includes these (from the zip) — verify, don't recreate
`package.json` (scripts: `build`, `watch`, `test`, `test:dist`), `build.js`, `watch.js`,
`.github/workflows/deploy.yml` (push-to-`main` → build + test + deploy to Pages), `.gitignore`,
and `FAST-WORKFLOW.md`. The jsdom suites live in `test/`.

## YOUR FIRST TASKS
1. If the repo still shows only the monolith, unpack `tempo-source.zip` into the repo root so the
   full `src/` + `build.js` + `test/` + `package.json` are present. Confirm the tree matches the
   "How the code is organized" section above.
2. `npm install` (installs jsdom, the only dev dependency).
3. `npm run build` then `npm test` — everything must be green with zero console errors. Confirm the
   freshly built `dist/index.html` matches the deployed `index.html` (the bundler is the source of truth).
4. Commit the source on a branch and open a PR: "Commit modular source + dev workflow (watch + CI)".
   In the PR note that `index.html` at root is now the generated bundle (output of `build.js`).
5. After merge, set repo **Settings → Pages → Source = GitHub Actions** so pushes auto-deploy.
6. Confirm the fast loop works: `npm run watch` in one terminal (rebuilds on save), `git` in another;
   `git push` triggers the CI deploy.

When done, summarize what changed and give me the exact commands for the fast loop. Then wait for my
next feature request. Remember the GOLDEN RULES above — especially: edit `src/**`, never `dist/`.
